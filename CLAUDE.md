## Project Context

This is a REPO mod that limits the number of upgrades that a player can have. This should be configurable per upgrade and have an option to enable/disable per upgrade. consuming an upgrade past the limit should no op. 
